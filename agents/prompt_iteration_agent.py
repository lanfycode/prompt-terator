"""
PromptIterationAgent — Phase 2 stub.

Will orchestrate the full multi-round automated prompt iteration loop
(generate test cases → batch test → analyse → optimise → re-test).
"""
from __future__ import annotations


class PromptIterationAgent:
    """Phase 2: Drives multi-round automated prompt iteration."""

    def iterate(self, *args, **kwargs):  # noqa: ANN002
        raise NotImplementedError("PromptIterationAgent is a Phase 2 component.")
